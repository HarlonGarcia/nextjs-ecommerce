import styled from "styled-components";

export const Container = styled.main`
  width: 100%;
  height: 100%;

  padding: 2rem;
  background-color: var(--black);

  h1 {
    color: var(--white);
  }
`;
